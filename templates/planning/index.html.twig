{% extends 'base.html.twig' %}

{% block title %}Planning{% endblock %}




{% block body %}

    <div id="AllPage-calendar">
        <!-- ##########_First bloc Planning_########## -->
        <div id="block-image-planning">
            <img id="Image-plannig-first" src="{{ asset('Directory/Image-plannig-first.png') }}" alt="Image-planning-sport">
            <h2 id="motive">Motivé ?</h2>
            <h2 id="go">GO !</h2>
        </div>

        <!-- ##########_Second bloc Planning_########## -->
        <a href="{{path('app_lesson_index')}}"> <h2 style="width:fit-content; color: white">Ajouter</h2></a>

        <table>
            <thead>
                <tr>
                    <th colspan="7" class="center">
                        {{ time|date('d M Y') }}
                    </th>
                </tr>
                <tr>
                    <th width="17%">Lundi</th>
                    <th width="17%">Mardi</th>
                    <th width="17%">Mercredi</th>
                    <th width="17%">Jeudi</th>
                    <th width="17%">Vendredi</th>
                    <th width="9%">Samedi</th>
                    <th width="9%">Dimanche</th>
                </tr>
            </thead>

            <tbody>
                <tr>
                    <td>
                        <section id="Calendar-section" style="color: white">
                            {% for lesson in lessons%}
                                {% if lesson.Day == 'Lundi' %}
                                    <div style="border: #DDDDDD solid 1px; width:fit-content; margin: 20px; background-color: {{ lesson.BackgroundColor }} ">
                                        <p>Name: {{ lesson.Name }}</p>
                                        <p>Coach: {{ lesson.Coach }}</p>
                                        <p>Location: {{ lesson.Location }}</p>
                                        <p>Place: {{ lesson.NbPlace }}</p>
                                    </div>
                                {% endif %}
                            {% endfor %}
                        </section>
                    </td>

                    <td>
                        <section id="Calendar-section" style="color: white">
                            {% for lesson in lessons%}
                                {% if lesson.Day == 'Mardi' %}
                                    <div style="border: #DDDDDD solid 1px; width:fit-content; margin: 20px; background-color: {{ lesson.BackgroundColor }} ">
                                        <p>Name: {{ lesson.Name }}</p>
                                        <p>Coach: {{ lesson.Coach }}</p>
                                        <p>Location: {{ lesson.Location }}</p>
                                        <p>Place: {{ lesson.NbPlace }}</p>
                                    </div>
                                {% endif %}
                            {% endfor %}
                        </section>
                    </td>

                    <td>
                        <section id="Calendar-section" style="color: white">
                            {% for lesson in lessons%}
                                {% if lesson.Day == 'Mercredi' %}
                                    <div style="border: #DDDDDD solid 1px; width:fit-content; margin: 20px; background-color: {{ lesson.BackgroundColor }} ">
                                        <p>Name: {{ lesson.Name }}</p>
                                        <p>Coach: {{ lesson.Coach }}</p>
                                        <p>Location: {{ lesson.Location }}</p>
                                        <p>Place: {{ lesson.NbPlace }}</p>
                                    </div>
                                {% endif %}
                            {% endfor %}
                        </section>
                    </td>
                    <td>
                        <section id="Calendar-section" style="color: white">
                            {% for lesson in lessons%}
                                {% if lesson.Day == 'Jeudi' %}
                                    <div style="border: #DDDDDD solid 1px; width:fit-content; margin: 20px; background-color: {{ lesson.BackgroundColor }} ">
                                        <p>Name: {{ lesson.Name }}</p>
                                        <p>Coach: {{ lesson.Coach }}</p>
                                        <p>Location: {{ lesson.Location }}</p>
                                        <p>Place: {{ lesson.NbPlace }}</p>
                                    </div>
                                {% endif %}
                            {% endfor %}
                        </section>
                    </td>
                    <td>
                        <section id="Calendar-section" style="color: white">
                            {% for lesson in lessons%}
                                {% if lesson.Day == 'Vendredi' %}
                                    <div style="border: #DDDDDD solid 1px; width:fit-content; margin: 20px; background-color: {{ lesson.BackgroundColor }} ">
                                        <p>Name: {{ lesson.Name }}</p>
                                        <p>Coach: {{ lesson.Coach }}</p>
                                        <p>Location: {{ lesson.Location }}</p>
                                        <p>Place: {{ lesson.NbPlace }}</p>
                                    </div>
                                {% endif %}
                            {% endfor %}
                        </section>
                    </td>
                    <td>
                        Rien
                    </td>
                    <td>
                        Rien
                    </td>
                </tr>
            </tbody>
        </table>
        {{dump(lessons) }}

        {#<section id="Calendar-section" style="color: white">
            {% for lesson in lessons%}
                <div style="border: #DDDDDD solid 1px; width:fit-content; margin: 20px; background-color: {{ lesson.BackgroundColor }} ">
                    <p>Start: {{ lesson.StartTime|date("m/d/Y H:m:s") }}</p>
                    <p>End: {{ lesson.EndTime|date("m/d/Y H:m:s") }}</p>
                    <p>Name: {{ lesson.Name }}</p>
                    <p>Coach: {{ lesson.Coach }}</p>
                    <p>Location: {{ lesson.Location }}</p>
                    <p>Place: {{ lesson.NbPlace }}</p>
                </div>
            {% endfor %}
        </section>#}
    </div>

    {% include 'Footer.html.twig' %}
{% endblock %}


