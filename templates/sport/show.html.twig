{% extends 'base.html.twig' %}

{% block title %}Sport{% endblock %}

{% block body %}
    <h1>Sport</h1>

    <table class="table-info">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ sport.Name }}</td>
            </tr>
        </tbody>
    </table>
    <div class="container-btn-show">
        <a class="btn-show-retour" href="{{ path('app_planning') }}">Retour</a>

        <a class="btn-show-save" href="{{ path('app_sport_edit', {'id': sport.id}) }}">edit</a>

        {{ include('sport/_delete_form.html.twig') }}
    </div>

{% endblock %}
